"""
Chapter 15: Generative Art & Cellular Automata

Pluggable automata and generative patterns that render directly to the
flipdot display buffer. Each generator is a class that produces frames
(7-row × 30-col grids) at a configurable tick rate.

The display becomes a tiny universe where you can *hear* each generation
click into place.
"""
